To make/compile the program:
 $make dog

Assumptions:
-mM is an invalid call
if you do '-m -M mapfile.txt', it will count that as an incorrect option

--help, will take precedence over any other flag.